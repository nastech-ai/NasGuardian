name: run_tests.yml (stub — see nasguardian_tests.yml)
# This workflow uses Android/Gradle and does not apply to NasGuardian (Python repo).
# Use nasguardian_tests.yml instead.
on:
  workflow_dispatch:
jobs:
  info:
    name: Redirect Notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This is a NasTerminal (Android) workflow."
          echo "For NasGuardian (Python), use: nasguardian_tests.yml"
          echo "See: https://github.com/nastech-ai/NasGuardian/actions"
